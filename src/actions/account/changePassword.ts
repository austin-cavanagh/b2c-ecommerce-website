// Not functional yet but is connected to the button in the ChangePassword.tsx component

'use server';
import 'server-only';

export async function changePassword() {
  console.log('change password');
}
